Flash Message stored in cookies and cleared when we referesh 
connect - flash

To send with response we will create our own coustom middleware

Steps
1.) Set up library 
2.) app.use(flash())
3.) set up messages 
4.) to set in res we created a custom middleware 
5.) We fetched data from req  and put into locals

We will use noty js